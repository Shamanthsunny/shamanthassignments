import React  from "react";


export default function Engine (props){

    return (
        <div>
            <h1>{props.model}</h1>
        <h2>{props.yom}</h2>
        </div>
        
    )
}